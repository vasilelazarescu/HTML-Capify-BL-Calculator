
  # Prepare Static HTML/CSS

  This is a code bundle for Prepare Static HTML/CSS. The original project is available at https://www.figma.com/design/cQ3kXqwF6t7qpS0mgyc6kS/Prepare-Static-HTML-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  